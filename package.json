{
  "name": "webgpu-game-of-life",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist node_modules"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1",
    "typescript": "^5.9",
    "vite": "^8.0"
  }
}
